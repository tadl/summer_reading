<h2> Summer Reading Club Progress </h2>
<% if @participants %>
	<% if @participants.count != 0 %>
		We found <%= pluralize(@participants.count, 'participant') %> registered with this account. <br>
		    <% @participants.each do |participant| %>
      <div id="patron_<%= participant.id %>"class="patron_card">  
        
        <h3> <%= participant.first_name %> <%= participant.last_name %> </h3> 
        <p> 
           <strong>Age:</strong> <%= participant.age %>, 
        </p>
        <p>
          <h3>Weekly Reports</h3>
          <ul id="week_list" style="list-style: none">
            <li> 
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-1" <%=week_helper(participant.week_1)%>> 
              Week 1 (June 19 - June 25) 
            </li>
            <li>
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-2" <%=week_helper(participant.week_2)%>>  
              Week 2 (June 26 - July 02) 
            </li>
            <li> 
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-3" <%=week_helper(participant.week_3)%>> 
              Week 3 (July 03 - July 09)
            </li>
            <li>
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-4" <%=week_helper(participant.week_4)%>>  
              Week 4 (July 10 - July 16)
            </li>
            <li>
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-5" <%=week_helper(participant.week_5)%>>  
              Week 5 (July 17 - July 23)
            </li>
            <li>
              <input type="checkbox" class="self_week_report" value="<%= participant.id %>-6" <%=week_helper(participant.week_6)%>> 
              Week 6 (July 24 - July 31)
            </li>
          </ul>
        </p>
        <% end %>
	<% else %>
		
		<p>Sorry. We didn't find any participants registered with your library card number.</p>
	
	    <p>Already registered but not seeing your progress here? Ask to add your library card the next time you come in to the library to report on your experiences!</p>
	
	    <p>Not yet registered? <a href="https://summer.tadl.org/">Sign up for Summer Reading</a>!</p>
	
	<% end %>
<% else %>
	<p>You must be logged into your library account to check your summer reading progress.</p>
	
	<p>Please <a href="https://summer.tadl.org/">login at catalog.tadl.org</a>.</p>
<% end %>
<div id="fancybox2" style="display:none">...</div>

